import { CoverBase } from '@basicClasses/cover/coverBase';
import { TestService } from '@portalBasicClasses/services/testService';

export class TestCover extends CoverBase {
  constructor(service: TestService) {
    super();
  }
}
